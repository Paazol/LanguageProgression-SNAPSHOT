import createRouter from "../../shared/routing/router.js";

const routes = [
    {
        path: "/login",
        element: <div>Login Page</div> // Replace with your actual login component
    }
];

createRouter(routes);